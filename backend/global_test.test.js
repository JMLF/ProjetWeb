// This passes because 1 === 1
test('Testing to see if Jest works', () => {
    expect(1).toBe(1)
  })

// This passes because 1 === 1
// test('Should not pass', () => {
//     expect(1).toBe(2)
//   })
